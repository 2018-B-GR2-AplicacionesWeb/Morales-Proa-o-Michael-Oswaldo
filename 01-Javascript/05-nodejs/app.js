//app.js
const node = require('./nodejs.js');
const runtime = require('./runtime');
const so = require('./so');
//const so = require('./util/util');
//const na = require ('../');
const fs = require('fs');

const express = require('express');
//console.log(node);
//console.log(runtime);
//console.log(so);
//console.log(so.imprimir);
console.log(express)


console.log(fs);